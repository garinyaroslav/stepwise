version: "3"

services:
  postgres-stepwise:
    image: postgres
    environment:
      - POSTGRES_DB=stepwise_db
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
    ports:
      - "5432:5432"
